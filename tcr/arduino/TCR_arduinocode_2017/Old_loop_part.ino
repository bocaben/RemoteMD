  /*if  (inputString.startsWith("R"))
    {
      digitalWrite(led, LOW);
      
      //Serial.println(inputString+"\n");
      while (inputString[0] == ' ' || inputString[0] == 'R')
      {
        for (int i = 0; i < inputString.length(); i++)
        {
          inputString[i] = inputString[i + 1];
        }
      }
      double th = inputString.toDouble();
      Serial.println(th);
      control_right_mot_angle(th);
      rightmotor = 0;
      leftmotor = 0;
      inputString="";
    }
    
    if  (inputString.startsWith("L"))
    {
      digitalWrite(led, LOW);
      
      Serial.println(inputString+"\n");
      while (inputString[0] == ' ' || inputString[0] == 'L')
      {
        for (int i = 0; i < inputString.length(); i++)
        {
          inputString[i] = inputString[i + 1];
        }
      }
      double th = inputString.toDouble();
      Serial.println(th);
      control_left_mot_angle(th);
      rightmotor = 0;
      leftmotor = 0;
      inputString="";
    }
*/
